# BPE-Core 模块需求文档

## 1. 功能需求

### 1.1 系统生命周期管理

| ID | 需求 | 优先级 | 状态 |
|----|------|--------|------|
| REQ-001 | 系统必须提供 `start()` 函数初始化所有组件 | 高 | 已实现 |
| REQ-002 | 初始化必须幂等，多次调用仅执行一次 | 高 | 已实现 |
| REQ-003 | 系统必须提供 `stop()` 函数标记停用状态 | 中 | 已实现 |
| REQ-004 | 组件初始化顺序必须正确：config→logger→jit→data→store→mapper→aggregate | 高 | 已实现 |

### 1.2 数据模型定义

| ID | 需求 | 优先级 | 状态 |
|----|------|--------|------|
| REQ-010 | 必须支持定义 `incoming` 类型记录 | 高 | 已实现 |
| REQ-011 | 必须支持定义 `stream` 类型记录 | 高 | 已实现 |
| REQ-012 | 记录必须支持 `Long` 类型列（64位整数） | 高 | 已实现 |
| REQ-013 | 记录必须支持 `Double` 类型列（64位浮点） | 高 | 已实现 |
| REQ-014 | 列名必须唯一，重复定义应警告 | 中 | 已实现 |
| REQ-015 | 记录ID必须自动生成，范围 1-65535 | 高 | 已实现 |

### 1.3 数据存储

| ID | 需求 | 优先级 | 状态 |
|----|------|--------|------|
| REQ-020 | 数据必须存储在环形缓冲区中 | 高 | 已实现 |
| REQ-021 | 缓冲区大小必须可配置 | 中 | 已实现 |
| REQ-022 | 记录大小必须可配置，最大8KB | 中 | 已实现 |
| REQ-023 | 写入操作必须是O(1)时间复杂度 | 高 | 已实现 |
| REQ-024 | 缓冲区满时必须覆盖最旧数据 | 高 | 已实现 |

### 1.4 SQL解析

| ID | 需求 | 优先级 | 状态 |
|----|------|--------|------|
| REQ-030 | 必须支持 SELECT 语句 | 高 | 已实现 |
| REQ-031 | 必须支持 WHERE 子句过滤 | 高 | 已实现 |
| REQ-032 | 必须支持 LIMIT 限制结果数量 | 中 | 已实现 |
| REQ-033 | 必须拒绝不支持的SQL特性（JOIN、GROUP BY等） | 高 | 已实现 |
| REQ-034 | SQL解析错误必须记录警告日志 | 中 | 已实现 |

### 1.5 JIT过滤

| ID | 需求 | 优先级 | 状态 |
|----|------|--------|------|
| REQ-040 | WHERE子句必须编译为原生机器码 | 高 | 已实现 |
| REQ-041 | 必须支持比较运算符：=, !=, >, <, >=, <= | 高 | 已实现 |
| REQ-042 | 必须支持逻辑运算符：AND, OR | 高 | 已实现 |
| REQ-043 | 必须支持算术运算符：+, -, *, /, % | 中 | 已实现 |
| REQ-044 | JIT过滤延迟必须小于1微秒 | 高 | 已实现 |
| REQ-045 | JIT编译失败必须返回错误 | 高 | 已实现 |

### 1.6 Mapper处理

| ID | 需求 | 优先级 | 状态 |
|----|------|--------|------|
| REQ-050 | 必须支持为记录定义多个Mapper | 高 | 已实现 |
| REQ-051 | 新数据到达时必须触发相关Mapper | 高 | 已实现 |
| REQ-052 | Mapper必须应用JIT过滤后调用回调 | 高 | 已实现 |
| REQ-053 | 回调必须异步执行（单线程内） | 中 | 已实现 |
| REQ-054 | 必须支持正向和反向遍历数据 | 中 | 已实现 |

### 1.7 聚合函数

| ID | 需求 | 优先级 | 状态 |
|----|------|--------|------|
| REQ-060 | 必须支持 `_maxl`/`_maxd` 最大值聚合 | 高 | 已实现 |
| REQ-061 | 必须支持 `_minl`/`_mind` 最小值聚合 | 高 | 已实现 |
| REQ-062 | 必须支持 `_suml`/`_sumd` 求和聚合 | 高 | 已实现 |
| REQ-063 | 必须支持 `_count` 计数聚合 | 高 | 已实现 |
| REQ-064 | 必须支持 `_avgl`/`_avgd` 平均值聚合 | 中 | 已实现 |
| REQ-065 | 必须支持 `_firstl`/`_firstd` 首值聚合 | 中 | 已实现 |
| REQ-066 | 必须支持 `_lastl`/`_lastd` 末值聚合 | 中 | 已实现 |
| REQ-067 | 聚合必须增量计算，避免全量遍历 | 高 | 已实现 |

### 1.8 回调机制

| ID | 需求 | 优先级 | 状态 |
|----|------|--------|------|
| REQ-070 | 必须支持Rust闭包作为回调 | 高 | 已实现 |
| REQ-071 | 必须支持FFI函数作为回调 | 中 | 已实现 |
| REQ-072 | 回调参数必须包含数据指针和元数据 | 高 | 已实现 |
| REQ-073 | 回调必须线程安全（Send trait） | 高 | 已实现 |

## 2. 非功能需求

### 2.1 性能需求

| ID | 需求 | 指标 | 状态 |
|----|------|------|------|
| NFR-001 | 数据插入延迟 | < 100ns | 已实现 |
| NFR-002 | JIT过滤延迟 | < 500ns | 已实现 |
| NFR-003 | 端到端延迟 | < 1μs | 已实现 |
| NFR-004 | 吞吐量 | > 100万记录/秒/核心 | 已实现 |

### 2.2 内存需求

| ID | 需求 | 指标 | 状态 |
|----|------|------|------|
| NFR-010 | 固定内存分配 | 启动时预分配 | 已实现 |
| NFR-011 | 无动态堆分配 | 核心路径 | 已实现 |
| NFR-012 | 内存占用 | 可配置上限 | 已实现 |

### 2.3 可靠性需求

| ID | 需求 | 指标 | 状态 |
|----|------|------|------|
| NFR-020 | 内存安全 | Rust所有权保证 | 已实现 |
| NFR-021 | 无数据竞争 | 无共享可变状态 | 已实现 |
| NFR-022 | 错误恢复 | 可恢复错误记录日志 | 已实现 |

### 2.4 可维护性需求

| ID | 需求 | 指标 | 状态 |
|----|------|------|------|
| NFR-030 | API稳定性 | 向后兼容 | 已实现 |
| NFR-031 | 代码可读性 | 充分注释 | 已实现 |
| NFR-032 | 测试覆盖 | 核心功能100% | 已实现 |

## 3. 约束条件

### 3.1 技术约束

| ID | 约束 | 说明 |
|----|------|------|
| CON-001 | Rust版本 | 2021 Edition |
| CON-002 | LLVM版本 | 19.x |
| CON-003 | 单表架构 | 不支持JOIN |
| CON-004 | 单线程核心 | 无并发控制 |

### 3.2 平台约束

| ID | 约束 | 说明 |
|----|------|------|
| CON-010 | 目标平台 | Linux x86_64 |
| CON-011 | 编译器 | rustc stable |

## 4. 接口需求

### 4.1 公共API

```rust
// 必须提供的公共函数
pub fn start();
pub fn stop();
pub fn def_incoming(name: &str, columns: Vec<Column>) -> Option<u16>;
pub fn def_stream(name: &str, columns: Vec<Column>) -> Option<u16>;
pub fn def_mapper<F>(sql: &str, func: F) -> Option<u16>;
pub fn def_aggregate<F>(sql: &str, func: F) -> Option<u16>;
pub fn new_data(data: &U8Bytes) -> bool;

// 必须提供的公共类型
pub struct Column;
pub struct U8Bytes;
pub struct CallbackParams;
pub trait FfiFunc;
```

### 4.2 配置接口

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `vec_size` | usize | 1048576 | 缓冲区大小(字节) |
| `record_size` | usize | 512 | 记录大小(字节) |
| `dev_mode` | bool | true | 开发模式 |

## 5. 验收标准

### 5.1 功能验收

- [ ] 所有单元测试通过
- [ ] 所有集成测试通过
- [ ] SQL解析正确
- [ ] JIT编译成功
- [ ] 数据正确过滤
- [ ] 聚合结果正确

### 5.2 性能验收

- [ ] 延迟达标
- [ ] 吞吐达标
- [ ] 内存稳定